mixin carouselItem(src, alt, title, desc, active=false)
  .carousel-item(class=active ? 'active' : '')
    .zoomable-img 
      img.d-block.w-100.gallery-img(src=src, alt=alt)
    .carousel-caption.d-none.d-md-block
      h5.font-weight-bold.text-uppercase= title
      p= desc
